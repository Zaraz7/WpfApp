<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.CommandBindings>
        <CommandBinding Command="New" Executed="NewExecute"/>
        <CommandBinding Command="New" Executed="OpenExecute"/>
        <CommandBinding Command="New" Executed="SaveExecute"/>
        <CommandBinding Command="New" Executed="CloseExecute"/>
        <CommandBinding Command="New" Executed="CutExecute"/>
        <CommandBinding Command="New" Executed="CopyExecute"/>
        <CommandBinding Command="New" Executed="PasteExecute"/>
    </Window.CommandBindings>
    <TabControl>
        <TabItem Name="tiExp1" Header="Задание 1-2">
            <DockPanel>
                <Menu DockPanel.Dock="Top" Height="22">
                    <MenuItem Header="Файл">
                        <MenuItem Command="New"/>
                        <MenuItem Command="Open"/>
                        <MenuItem Command="Save"/>
                        <MenuItem Command="Close"/>
                    </MenuItem>
                    <MenuItem Header="Правка">
                        <MenuItem Command="Cut"/>
                        <MenuItem Command="Copy"/>
                        <MenuItem Command="Paste"/>
                    </MenuItem>
                </Menu>
                <TextBox/>
            </DockPanel>
        </TabItem>
        <TabItem Header="Задание 3">
            
        </TabItem>
    </TabControl>
</Window>
